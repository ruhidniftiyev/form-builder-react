import React from 'react';
import ElementsList from '../components/ElementsList';

type Props = {};

const Home = (props: Props) => {
  return (
    <div>
      <ElementsList />
    </div>
  );
};

export default Home;
