import React from 'react';

type Props = {
  form: any;
};

const FormBuilder = ({ form }) => {
  return (
    <div>
      <form>
        
      </form>
    </div>
  );
};

export default FormBuilder;
